explicit
Label (
const Glib::ustring& label,
bool mnemonic = false
);

Label (
const Glib::ustring& label,
float xalign, float yalign,
bool mnemonic = false
);

Label (
const Glib::ustring& label,
Align xalign,
Align yalign = ALIGN_CENTER,
bool mnemonic = false
);
